function validatePIN (pin) {
    if(pin === 3 && pin === 5){
    console.log(`true`);
   
   } else {
   console.log(`false`);
   }
}

Number.isInteger


function validatePIN (pin) {
    if (reg = /ˆ(\d{4}|\d{6})$/g.test);
}
    return reg.test('1234');
    
    }
    console.log(validatePIN(1234));



    function validate(pin){
    test = /^(\d{6}|\d{4}$)/g.test(pin)
    return test;
 }
        
    

 function queueTime(customers, n) {
    if (toString.call(customers) !== "[object Array]")
       return false;
         
               var total =  0;
               for(var i=0;i<customers.length;i++)
                 {                  
                   if(isNaN(customers[i])){
                   continue;
                    }
                     total += Number(n / (customers[i]));
                  }
                return total;
               } //TODO

               queueTime([1,2,3,4,5], 100)
       
        
        
        
        